//Write a function that checks if a given number is a three digit long number.

function treeDigitNumber(num) {
    if (num > 99 && num < 1000) {
        console.log(num + " is tree digit number.")
    }
}
treeDigitNumber(700);